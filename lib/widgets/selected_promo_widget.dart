import 'package:flutter/material.dart';

class SelectedPromoWidget extends StatelessWidget {
  const SelectedPromoWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
